# ============================================
# Session 6 Lab: Environment Variables
# ============================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# IMPORTANT: Never commit your .env file to git!
# ============================================

# Server port
PORT=3000

# PostgreSQL connection URL
DATABASE_URL=postgresql://user:password@localhost:5432/webdev_db

# ============================================
# JWT (JSON Web Token) Configuration
# ============================================
# JWT_SECRET: A long, random string used to sign and verify tokens.
# This MUST be kept secret! Anyone with this key can forge tokens.
# Generate a good secret with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_secret_key_here

# JWT_EXPIRES_IN: How long tokens are valid before they expire.
# Examples: "1h" (1 hour), "24h" (24 hours), "7d" (7 days)
JWT_EXPIRES_IN=24h
